local a = game:GetService("TweenService")
local b = game:GetService("CoreGui")
local c = game:GetService("Players")
local d = c.LocalPlayer;
if setclipboard then
	setclipboard([[
📌 Owners: IDK HUB
🔗 Discord: later
🔗 YouTube: https://www.youtube.com/@IDK_Hub
]])
else
	warn("setclipboard() is not available in this shit.")
end;
if b:FindFirstChild("CreditGUI") then
	b:FindFirstChild("CreditGUI"):Destroy()
end;
local e = Instance.new("ScreenGui")
e.Name = "CreditGUI"
e.ZIndexBehavior = Enum.ZIndexBehavior.Sibling;
e.IgnoreGuiInset = true;
e.ResetOnSpawn = false;
e.Parent = b;
local f = Instance.new("Frame")
f.Name = "MainFrame"
f.AnchorPoint = Vector2.new(0.5, 0.5)
f.Position = UDim2.new(0.5, 0, 0.4, 0)
f.BackgroundTransparency = 1;
f.Size = UDim2.new(0, 520, 0, 0)
f.AutomaticSize = Enum.AutomaticSize.Y;
f.Parent = e;
local g = Instance.new("UIListLayout")
g.FillDirection = Enum.FillDirection.Vertical;
g.VerticalAlignment = Enum.VerticalAlignment.Center;
g.HorizontalAlignment = Enum.HorizontalAlignment.Center;
g.Padding = UDim.new(0, 8)
g.SortOrder = Enum.SortOrder.LayoutOrder;
g.Parent = f;
local function h(i)
	local j = Instance.new("TextLabel")
	j.AutomaticSize = Enum.AutomaticSize.Y;
	j.Size = UDim2.new(1, - 40, 0, 0)
	j.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
	j.TextColor3 = Color3.fromRGB(255, 255, 255)
	j.Text = i;
	j.TextSize = 18;
	j.Font = Enum.Font.GothamSemibold;
	j.TextWrapped = true;
	j.BackgroundTransparency = 1;
	j.TextTransparency = 1;
	j.ClipsDescendants = true;
	j.ZIndex = 10;
	j.Parent = f;
	Instance.new("UICorner", j).CornerRadius = UDim.new(0, 10)
	local k = Instance.new("UIStroke", j)
	k.Color = Color3.fromRGB(255, 255, 255)
	k.Thickness = 1.5;
	k.Transparency = 1;
	k.ApplyStrokeMode = Enum.ApplyStrokeMode.Border;
	return j, k
end;
local l, m = h("Owner credit copied to clipboard!")
local n, o = h("Script made by IDK Hub")
local function p(j, k)
	a:Create(j, TweenInfo.new(0.4), {
		BackgroundTransparency = 0.3,
		TextTransparency = 0
	}):Play()
	a:Create(k, TweenInfo.new(0.4), {
		Transparency = 0.25
	}):Play()
end;
p(l, m)
p(n, o)
task.wait(3.5)
local function q(j, k)
	a:Create(j, TweenInfo.new(0.5), {
		BackgroundTransparency = 1,
		TextTransparency = 1
	}):Play()
	a:Create(k, TweenInfo.new(0.5), {
		Transparency = 1
	}):Play()
end;
q(l, m)
q(n, o)
task.wait(0.6)
e:Destroy()
